package main;

import java.util.Comparator;

public class RequestItemComparator implements Comparator<RequestItem> {

    /**
     * TODO:
     * STUDENT INSTRUCTION:
     * Implement the this method to define comparison of RequestItems on additional
     * metrics. You may choose to use this class as a means to order elements in your
     * server job queue.
     *
     * @param o1 The first RequestItem.
     * @param o2 The second RequestItem.
     * @return An int corresponding to o1-o2.
     */
    @Override
    public int compare(RequestItem o1, RequestItem o2) {

        return 0;
    }
}
